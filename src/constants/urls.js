const baseURL = 'https://jsonplaceholder.typicode.com'

const users = '/users'
const userComments = '/comments'

const urls = {
    users:{
        base: users
    },
    userComments:{
        base: userComments
    }
}

export {
    baseURL,
    urls
}